{"findings": [{"body": "The existing AGENTS.md still frames the repo as the `core-template-cfw-assets-astro-shadcn` template, but the README, package name, and file tree show this is the `google-workspace-mcp` project (Google Workspace extension for Gemini CLI + Cloudflare Worker MCP server). The overview and architecture sections need to be re-centered on Google Workspace / MCP.", "code_location": "AGENTS.md", "confidence_score": 0.95, "priority": 1, "title": "Project identity mismatch"}, {"body": "Several `.agent/rules/*.md` files exist in the repo but are not referenced in AGENTS.md: `repository-init.md` is never mentioned, and `architecture.md`, `frontend-error-handling.md`, etc. are only partially surfaced. A comprehensive index of all rule files should be added so agents know which rule governs which concern.", "code_location": ".agent/rules/", "confidence_score": 0.9, "priority": 2, "title": "Incomplete rule references"}, {"body": "The codebase contains Google Workspace agents (`src/backend/ai/agents/{calendar,docs,drive,gmail,sheets,appscript,orchestrator}/`), per-agent `SKILL.md` files, and top-level `skills/` docs, plus `commands/*.toml` tool definitions. AGENTS.md lacks directives for agent structure, skill docs, and command TOML synchronization.", "code_location": "src/backend/ai/agents/", "confidence_score": 0.92, "priority": 1, "title": "Missing Google Workspace agent conventions"}, {"body": "The repo has D1 migrations under `drizzle/`, a `drizzle.config.ts`, and deploy/migrate scripts. AGENTS.md does not state that migrations are additive/immutable or how to generate/apply them, which risks migration rewrites in a multi-agent workflow.", "code_location": "drizzle/", "confidence_score": 0.88, "priority": 2, "title": "Missing D1 migration policy"}, {"body": "The MCP server (`/mcp` JSON-RPC), OAuth setup, KV/D1 bindings, `wrangler secret put`, and `scripts/create-worker-secrets.mjs` are central to this project but not covered in agent directives. Agents need guidance on stateless MCP design and secret handling.", "code_location": "src/_worker.ts, scripts/create-worker-secrets.mjs, README.md", "confidence_score": 0.9, "priority": 1, "title": "Missing MCP/OAuth directives"}, {"body": "The repo uses `gemini-extension.json`, `.mcp.json`, and a `cloud_function/` directory. These project-specific artifacts should be acknowledged so agents do not ignore or break them.", "code_location": "gemini-extension.json, .mcp.json, cloud_function/", "confidence_score": 0.75, "priority": 3, "title": "Project-specific artifacts not documented"}, {"body": "The existing `Template App Surface` section describes dashboard/projects/tasks/notes/inbox/chat/showcase pages. While these may still exist in the truncated tree, the primary app surface for this repo is Google Workspace + MCP. The section should be reframed to highlight Google Workspace agents and MCP while preserving the shared Astro/Shadcn foundation details.", "code_location": "AGENTS.md", "confidence_score": 0.85, "priority": 2, "title": "App surface section needs reframing"}, {"body": "The `client:only=\"react\"` rule for Agents SDK islands is buried in the app surface section. It should also be a numbered mandatory directive because violating it breaks SSR routes.", "code_location": "AGENTS.md", "confidence_score": 0.8, "priority": 3, "title": "Agents SDK island rule not prominent enough"}, {"body": "The repo has VitePress docs under `docs/` and release notes. AGENTS.md should direct agents to keep these updated, especially `docs/development.md`, `docs/feature-configuration.md`, and `docs/release_notes.md`.", "code_location": "docs/", "confidence_score": 0.78, "priority": 3, "title": "Missing documentation maintenance directive"}], "overall_confidence_score": 0.88, "overall_correctness":  "patch is correct", "overall_explanation": "The existing AGENTS.md is well-structured but reads like a generic template guide. The updated version keeps the same headings and most existing directives, while re-centering the project identity on `google-workspace-mcp`, adding Google Workspace agent conventions, MCP/OAuth/secret handling, D1 migration policy, skill/command sync rules, and references to all `.agent/rules/` files. It also reframes the app surface section to lead with Google Workspace agents and MCP, preserving the Astro/Shadcn/SSR/health/logging guidance that still applies."}